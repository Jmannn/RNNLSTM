public class LTSM {
    /* These are the variables for the forward prop results. Only includes the
     * extra variables required by RNN class. Since python, allows for multiple variable returns, this
     * was a workaround I chose. */
    double[] fFP, iFP, cFP, oFP;



    public void forwardProp(){

    }
}
